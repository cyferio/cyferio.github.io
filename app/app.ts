import { m } from "@ckzero/maya/web";

const HomePage = () => {
  return m.Div({
    children: [
      m.H2({
        innerText: "cyfers",
      }),
      m.H2({
        innerText: "cyfers",
      }),
    ],
  });
};

export const app = HomePage;
